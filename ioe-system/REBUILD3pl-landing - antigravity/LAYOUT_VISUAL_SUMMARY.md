# TMS & WMS Layout - Visual Summary

## Quick Reference Layout Structure

### Overall Application Structure

```
┌──────────────────────────────────────────────────────────────┐
│  Header: Logo | Breadcrumbs | Search | User Menu            │
├──────────────┬───────────────────────────────────────────────┤
│              │                                               │
│  LEFT        │  MAIN CONTENT                                 │
│  SIDEBAR     │  ┌─────────────────────────────────────────┐ │
│  (240px)     │  │                                         │ │
│              │  │  • DocType Forms (Create/Edit)         │ │
│  [Nav Items] │  │  • List Views (Tables with filters)    │ │
│              │  │  • Dashboards (KPIs + Visualizations)  │ │
│              │  │  • Reports (Analytics)                 │ │
│              │  │                                         │ │
│              │  │  ALL DATA FROM REAL APIs:              │ │
│              │  │  • GET /api/tms/*                      │ │
│              │  │  • GET /api/wms/*                      │ │
│              │  │  • Prisma Database                     │ │
│              │  │  • NO MOCK DATA                        │ │
│              │  └─────────────────────────────────────────┘ │
│              │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

---

## TMS Workspace Layout

### Page Structure

```
/tms (Workspace Home)
  │
  ├── /tms/dashboard
  │   └── KPIs + Map + Recent Shipments (Real Data)
  │
  ├── /tms/shipment
  │   ├── List View (GET /api/tms/shipments)
  │   └── Form View (POST/PATCH /api/tms/shipments/{id})
  │
  ├── /tms/order
  │   ├── List View (GET /api/orders)
  │   └── Form View (POST/PATCH /api/orders/{id})
  │
  ├── /tms/carrier
  │   ├── List View (GET /api/tms/carriers)
  │   └── Form View (POST/PATCH /api/tms/carriers/{id})
  │
  ├── /tms/load-plan
  │   └── Form with 3D Optimizer (POST /api/tms/load-plan)
  │
  └── /tms/reports/*
      └── Analytics Reports (GET /api/tms/analytics)
```

---

## WMS Workspace Layout

### Page Structure

```
/wms (Workspace Home)
  │
  ├── /wms/dashboard
  │   └── KPIs + 3D Warehouse + Active Tasks (Real Data)
  │
  ├── /wms/warehouse
  │   └── Form with 3D Configuration (GET/POST /api/warehouse)
  │
  ├── /wms/receiving
  │   ├── List View (GET /api/wms/receiving)
  │   └── Form View (POST /api/wms/receiving/{id}/confirm)
  │
  ├── /wms/inventory
  │   ├── List View (GET /api/wms/inventory)
  │   └── Form View (PUT /api/wms/inventory/{id})
  │
  ├── /wms/picking
  │   ├── List View (GET /api/wms/picking)
  │   └── Form View (POST /api/wms/picking/{id}/confirm)
  │
  └── /wms/reports/*
      └── Analytics Reports (GET /api/wms/analytics)
```

---

## Key Visual Elements

### Sidebar Navigation Style

```
┌────────────────────────┐
│ 🚚 Transportation      │  ← Workspace Header
├────────────────────────┤
│ 🏠 Home                │  ← Active state (blue highlight)
│ 📊 Dashboard           │
│                        │
│ 📦 Shipment            │  ← Regular state
│ 📄 Order               │
│                        │
│ 📋 Reports ▼           │  ← Collapsible section (open)
│    ├─ Analytics        │  ← Child item (indented)
│    └─ Performance      │
│                        │
│ ⚙️  Settings           │
└────────────────────────┘
```

### Form Layout Style (ERPNext Pattern)

```
┌─────────────────────────────────────────────┐
│  Breadcrumbs: Home > Module > DocType      │
├─────────────────────────────────────────────┤
│  [Save] [Submit] [Cancel] [Actions ▼]     │
├─────────────────────────────────────────────┤
│                                             │
│  Section Header (Gray Background Bar)      │
│  ┌───────────────────────────────────────┐ │
│  │ Field 1: [Input]                      │ │
│  │ Field 2: [Select ▼]                   │ │
│  │ Field 3: [Date Picker]                │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Child Table Section                       │
│  ┌───────────────────────────────────────┐ │
│  │ [+ Add Row]                           │ │
│  │ ┌─────────────────────────────────┐   │ │
│  │ │ Col 1 │ Col 2 │ Col 3 │ Actions│   │ │
│  │ ├─────────────────────────────────┤   │ │
│  │ │ Data │ Data  │ Data  │ [Edit]  │   │ │
│  │ └─────────────────────────────────┘   │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### Dashboard Layout Style

```
┌─────────────────────────────────────────────┐
│  Dashboard Title                            │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐     │
│  │ KPI  │ │ KPI  │ │ KPI  │ │ KPI  │     │
│  │ Card │ │ Card │ │ Card │ │ Card │     │
│  └──────┘ └──────┘ └──────┘ └──────┘     │
│                                             │
│  ┌──────────────────┐ ┌──────────────────┐ │
│  │                  │ │                  │ │
│  │  Chart/Map       │ │  Chart/List      │ │
│  │  Visualization   │ │  Visualization   │ │
│  │                  │ │                  │ │
│  └──────────────────┘ └──────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Frontend   │────────▶│  API Routes  │────────▶│   Prisma     │
│  (React UI)  │         │ (Next.js)    │         │   Database   │
└──────────────┘         └──────────────┘         └──────────────┘
      │                         │                         │
      │                         │                         │
      │  GET /api/tms/*    ────┼──────────────────────┼──
      │                         │                         │
      │  POST /api/tms/*   ────┼──────────────────────┼──
      │                         │                         │
      │  Real-time Updates      │                         │
      │  (TanStack Query)       │                         │
      │                         │                         │
      ▼                         ▼                         ▼
  ┌─────────────────────────────────────────────────────────┐
  │  NO MOCK DATA - ALL REAL DATABASE QUERIES              │
  └─────────────────────────────────────────────────────────┘
```

---

## Professional UI Color Palette

```
Primary Blue:    #2563eb  ━━━━━━━━━━━━━━━━━━━━━━
Background:      #ffffff  ━━━━━━━━━━━━━━━━━━━━━━
Sidebar BG:      #f8f9fa  ━━━━━━━━━━━━━━━━━━━━━━
Border:          #e5e7eb  ━━━━━━━━━━━━━━━━━━━━━━
Text Primary:    #111827  ━━━━━━━━━━━━━━━━━━━━━━
Text Secondary:  #6b7280  ━━━━━━━━━━━━━━━━━━━━━━
Success:         #10b981  ━━━━━━━━━━━━━━━━━━━━━━
Warning:         #f59e0b  ━━━━━━━━━━━━━━━━━━━━━━
Error:           #ef4444  ━━━━━━━━━━━━━━━━━━━━━━
```

---

## File Structure Preview

```
app/
├── tms/
│   ├── workspace.json          # Navigation config
│   ├── page.tsx                # Workspace home
│   ├── dashboard/page.tsx      # Dashboard
│   ├── shipment/page.tsx       # Shipment form/list
│   ├── order/page.tsx          # Order form/list
│   └── ...
├── wms/
│   ├── workspace.json          # Navigation config
│   ├── page.tsx                # Workspace home
│   ├── dashboard/page.tsx      # Dashboard
│   ├── receiving/page.tsx      # Receiving form/list
│   └── ...
└── ...

components/
├── workspace/                  # ERPNext-style components
│   ├── WorkspaceSidebar.tsx
│   ├── DocTypeForm.tsx
│   └── DocTypeList.tsx
└── ...

lib/
├── workspace/                  # Workspace system
│   ├── workspace-config.ts
│   └── doctype-registry.ts
└── ...
```

---

This is the complete professional, full-stack layout with zero mock data and enterprise-grade UI matching ERPNext!

